
import { ToastContainer } from "react-toastify";
import Routing from "./Routing";
// import Routing from "./Routing.jsx";

const app = () => {
	return (
		<>
			<ToastContainer />
			<Routing />
		</>
	);
};

export default app;

